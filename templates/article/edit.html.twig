{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <h1>Edit Article</h1>

    {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('article_show', {'id': article.id}) }}">back to list</a>

    <!-- if statut == admin -->
    {{ include('article/_delete_form.html.twig') }}
    <!-- endif -->
{% endblock %}
